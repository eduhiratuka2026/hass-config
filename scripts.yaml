encontrar_o_s23_ultra_do_edu:
  sequence:
  - action: notify.mobile_app_s23_ultra_do_edu
    data:
      message: command_ringer_mode
      data:
        command: normal
  - action: notify.mobile_app_s23_ultra_do_edu
    metadata: {}
    data:
      message: Telefone tocando no volume máximo solicitado pelo Home Assistant
      title: Encontre Meu Telefone do Home Assistant
      data:
        ttl: 0
        priority: high
        channel: alarm_stream
  alias: Encontrar o S23 Ultra do Edu
  icon: mdi:cellphone-marker
  description: 'Tocar o telefone S23 Ultra do Edu no volume máximo para que possa
    ser encontrado facilmente.

    '
enviar_tts_para_o_s23_ultra_do_edu:
  sequence:
  - if:
    - condition: template
      value_template: '{{ mensagem_de_voz is defined and mensagem_de_voz is string
        and mensagem_de_voz|length > 0 }}'
    then:
    - alias: envie uma mensagem usando as variáveis
      action: notify.mobile_app_s23_ultra_do_edu
      metadata: {}
      data:
        data:
          tts_text: '{{ mensagem_de_voz }}'
          media_stream: alarm_stream_max
          ttl: 0
          priority: high
        message: TTS
  alias: Enviar TTS para o S23 Ultra do Edu
  icon: mdi:cellphone-message
  description: 'Envia uma mensagem de voz que será tocada no S23 Ultra do Edu

    '
  fields:
    mensagem_de_voz:
      selector:
        text:
      name: Mensagem de Voz
      required: false
    campo:
      selector:
        text:
aplicar_efeito_no_abajur:
  sequence:
  - variables:
      efeito: '{{ state_attr(''light.abajur'', ''effect'') }}'
      novo_efeito: '{{ ''Scene'' if efeito == ''off'' else ''off'' }}'
  - action: light.turn_on
    metadata: {}
    target:
      entity_id: light.abajur
    data:
      effect: '{{ novo_efeito }}'
  fields: {}
  alias: Aplicar Efeito no Abajur
  description: ''
  icon: mdi:floor-lamp
